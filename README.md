So_long: a mini 2D game that introduces concepts like backtracking and the use of MiniLibX to create windows, handle images, and display pixels through code.

Push_swap: a tough but rewarding project that teaches the real struggles of coding 😅. the goal is to build a sorting algorithm that follows specific rules, testing it with anywhere from 100 to 5000 numbers to find the most efficient way to sort them using limited moves.

Pipex: focuses on understanding how pipes work in a shell by recreating their behavior; connecting processes, handling input/output, and simulating the magic behind | (pipe ) in bash.
